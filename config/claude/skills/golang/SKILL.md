---
name: golang
description: Use this skill when you're working with golang code.
---

- prefer switch statements over if/else
- be aware of build tags, like `//go:build unit_test`,
  if these are present make sure you run go build and test commands with them
- use guard statements over lengthy if/else
