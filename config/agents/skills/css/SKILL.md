---
name: css
description: Use this skill when you're working with CSS.
---

- define base font-size as var in `rem` and use calc to size text relatively
- use `em` for letter-spacing
- try avoiding pixels in general
