"""Unit tests for the transport-neutral ``notebooklm._app`` package.

A package (not a bare directory) so module basenames here can collide with
basenames elsewhere in ``tests/unit`` without confusing pytest's rootdir
import mode.
"""
