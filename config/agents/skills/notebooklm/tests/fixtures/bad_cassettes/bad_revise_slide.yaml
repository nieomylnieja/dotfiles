# Synthetic-bad fixture for C2 regression: f.req body was over-scrubbed to a
# literal "SCRUBBED" sentinel, destroying the urlencoded JSON payload that the
# shape-lint expects to decode.
#
# When the cassette-shape lint is reintroduced for this class of defect, the
# lint MUST fail on this fixture (assertion B: f.req must URL-decode + JSON-parse).
interactions:
- request:
    body: f.req=SCRUBBED&at=SCRUBBED_CSRF
    headers:
      Content-Type: [application/x-www-form-urlencoded]
    method: POST
    uri: https://notebooklm.google.com/_/LabsTailwindUi/data/batchexecute?rpcids=KmcKPe&source-path=%2Fnotebook%2Fvcr_test_notebook_id_sentinel_32ch&f.sid=SCRUBBED&rt=c
  response:
    body:
      string: ")]}'\n\n55\n[[\"wrb.fr\",\"KmcKPe\",\"[\\\"SCRUBBED\\\"]\",null,null,null,1]]\n"
    headers:
      Content-Type: [application/json; charset=utf-8]
    status: {code: 200, message: OK}
version: 1
